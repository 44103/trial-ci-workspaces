version: 2.1

jobs:
  # build:
  #   docker:
  #     - image: circleci/rust
  #   steps:
  #     - checkout
  #     - run:
  #         name: Build Lambda Functions
  #         command: make ci-build
  #     - save_cache:
  #         key: rust-{{ checksum "Cargo.lock" }}
  #         paths:
  #           - infrastructure/functions
  stage:
    docker:
      - image: hashicorp/terraform
    steps:
      - checkout
      - run:
          name: Stage AWS
          command: ./ci-stage.sh ${CIRCLE_BRANCH} ${COMMIT_MESSAGE}
          environment:
            COMMIT_MESSAGE: git log --format=oneline -n 1 $CIRCLE_SHA1


workflows:
  # build:
  #   jobs:
  #     - build
  stage:
    jobs:
      - stage
    # requires:
    #   - build
